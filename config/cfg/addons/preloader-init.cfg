// Cueki's Pre-loader init addon
// Used to pre-load custom content alongside Cueki's Casual Pre-loader
// Requires disabling the Dynamic Background module
// Note to self: investigate why it has be a one-liner

sv_pure -1; sv_allow_point_servercommand always; map itemtest; wait 10; script_execute randommenumusic; disconnect; wait 1; clear; exec comfig/echo; exec quickprecache.cfg; playmenumusic
